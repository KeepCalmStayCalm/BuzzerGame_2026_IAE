# GPIo config still NOK

- terminal: `pinctrl get 26` works, after `pinctrl set 26 pd`
- python wit hgpiozero: same, works after `pinctrl set 26 pd`
- java: still don't see nothin in JFX UI
- see https://www.raspberrypi.com/documentation/computers/config_txt.html#gpio-control